- name: Install fail2ban
  apt:
    name: fail2ban
    state: present

- name: Write fail2ban configuration
  ansible.builtin.template:
    src: fail2ban/jail.local.j2
    dest: /etc/fail2ban/jail.local
    mode: "0644"
  notify: "restart fail2ban"

- name: Flush handlers
  meta: flush_handlers
